/**
 * Optional runtime override for web app API base.
 * Copy to mobile-config.js and set your deployed /v1 URL.
 * mobile-config.js is gitignored — keep environment-specific values local.
 */
window.SAFEALERT_API = 'https://api.yourdomain.com/v1';
