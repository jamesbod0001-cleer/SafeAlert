rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All client access goes through the Admin SDK API — deny direct client writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
